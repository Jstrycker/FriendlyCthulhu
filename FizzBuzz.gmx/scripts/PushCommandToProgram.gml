action_type = argument0;
x_pos = argument1;
y_pos = argument2;

//example of using the variables
instance_create(x_pos, y_pos, action_type);

//either push this to the player stack and then call run, or create an object or array to store them as you build the stack and the push to the player when you want to run

